\documentclass[
$if(fontsize)$
    $fontsize$,
$endif$
$if(papersize)$
    $papersize$
$endif$
]{scrartcl}  

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
\usepackage{fancyvrb}
\VerbatimFootnotes
\usepackage{array}
\newcommand{\PreserveBackslash}[1]{\let\temp=\\#1\let\\=\temp}
\let\PBS=\PreserveBackslash
\newcommand{\textsubscr}[1]{\ensuremath{_{\scriptsize\textrm{#1}}}}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage[breaklinks=true]{hyperref}
\hypersetup{hidelinks}
\usepackage{url}

%%%%% Language %%%%%
$if(lang)$
\ifLuaTeX
\usepackage[bidi=basic]{babel}
\else
\usepackage[bidi=default]{babel}
\fi
$if(babel-lang)$
\babelprovide[main,import]{$babel-lang$}
$endif$
$for(babel-otherlangs)$
\babelprovide[import]{$babel-otherlangs$}
$endfor$
\let\LanguageShortHands\languageshorthands
\def\languageshorthands#1{}
$endif$


%%%%% Image Support %%%%%
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
\makeatletter
\def\fps@figure{htbp}
\makeatother

%%%%% Table Support %%%%%
\usepackage{longtable,booktabs,array}
\usepackage{multirow}
\usepackage{calc}
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}


%%%%% bibliography support %%%%%
\usepackage{biblatex}
\usepackage{csquotes}
$for(bibliography)$
\addbibresource{$bibliography$}
$endfor$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newlength{\cslentryspacingunit}
\setlength{\cslentryspacingunit}{\parskip}
\newenvironment{CSLReferences}[2]{
  \setlength{\parindent}{0pt}
  \ifodd #1
  \let\oldpar\par
  \def\par{\hangindent=\cslhangindent\oldpar}
  \fi
  \setlength{\parskip}{#2\cslentryspacingunit}
 }{}
\usepackage{calc}
\newcommand{\CSLBlock}[1]{#1\hfill\break}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{#1}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{#1}\break}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}


%%%%% Page Style %%%%%
\usepackage[headsepline,footsepline]{scrlayer-scrpage}
\newpairofpagestyles{main-pagestyle}{
  \clearpairofpagestyles
  \ihead{\headmark}
  \automark{section}
  \ofoot{\thepage}
  \addtokomafont{pageheadfoot}{\upshape}
}
\pagestyle{main-pagestyle}


%%%%% Font Style %%%%%
\usepackage[default]{sourcesanspro}
\usepackage{sourcecodepro}
\ifxetex
    \makeatletter
    \defaultfontfeatures[\ttfamily]
      { Numbers   = \sourcecodepro@figurestyle,
        Scale     = \SourceCodePro@scale,
        Extension = .otf }
    \setmonofont
      [ UprightFont    = *-\sourcecodepro@regstyle,
        ItalicFont     = *-\sourcecodepro@regstyle It,
        BoldFont       = *-\sourcecodepro@boldstyle,
        BoldItalicFont = *-\sourcecodepro@boldstyle It ]
      {SourceCodePro}
    \makeatother
\fi


%%%%% Meta Data %%%%%
\author{$for(author)$$author$$sep$\\$endfor$}
$if(title)$
    \title{$title$}
$endif$
$if(date)$
    \date{$date$}
$endif$


%%%%% includes support %%%%%
$for(header-includes)$
    $header-includes$
$endfor$


%%%%% Document Start %%%%%
\begin{document}

%%%%% Title Page %%%%%
\begin{titlepage}
    \begin{center}
        $if(institution)$
            {\scshape\huge $institution$ \par}
            \vspace{0.5cm}
        $endif$
  
        $if(faculty)$
            {\scshape\Large $faculty$ \par}
        $endif$
    
        $if(course)$
            {\scshape\large $course$ \par}
        $endif$

        \vspace{2.0cm}

        $if(title)$
            $for(title)$
                {\LARGE\bfseries $title$ \par}
            $endfor$
        $endif$

        \vspace{1.5cm}

        $if(subtitle)$
            {\scshape\large\bfseries $subtitle$ \par}
        $endif$
        
        \vspace{1.5cm}

        $if(author)$
            {\scshape\normalsize Author: $author$ \par}
        $endif$

        \vspace{1.5cm}

        $if(date)$
            {\scshape\normalsize Datum: $date$ \par}
        $endif$

      \end{center}
\end{titlepage}


%%%%% Abstract %%%%%
$if(abstract)$
    \thispagestyle{plain}
    \begin{abstract}
        $abstract$
    \end{abstract}
    \clearpage
$endif$


%%%%% Declaration %%%%%
\thispagestyle{plain}
\section*{Eidesstattliche Erklärung}

Ich versichere hiermit an Eides Statt durch meine Unterschrift, dass ich die vorliegende Arbeit selbstständig und ohne fremde Hilfe angefertigt habe.
Alle Stellen, die wörtlich oder dem Sinn nach auf Publikationen oder Vorträgen anderer Autoren beruhen, sind als solche kenntlich gemacht.
Ich versichere außerdem, dass ich keine anderen als die angegebenen Quellen verwendet habe.
Diese Versicherung bezieht sich auch auf alle in der Arbeit enthaltenen Tabellen, Abbildungen und dergleichen.

Die Arbeit wurde bisher keiner weiteren Prüfungsbehörde vorgelegt und auch noch nicht veröffentlicht.

$if(author)$
$if(place)$
$if(date)$
    \vspace{2.5cm}
    $place$, den $date$\newline
    $author$
$endif$
$endif$
$endif$
\clearpage


%%%%% Table of Contents %%%%%
$if(toc)$
    \setcounter{secnumdepth}{5}
    \tableofcontents
    \clearpage

    $if(graphics)$
        \listoffigures
        \addcontentsline{toc}{section}{\listfigurename}
        \clearpage
    $endif$

    $if(tables)$
        \listoftables
        \addcontentsline{toc}{section}{\listtablename}
        \clearpage
    $endif$
$endif$


$body$



\end{document}